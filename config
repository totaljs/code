name                  : Code
allow_custom_titles   : true

cookie   (generate)   : 10
authkey  (generate)   : 10

backup                : /www/tmp/backup/
autodiscover          : /www/www/

allow_tms             : false

folder_www     (env)  : FOLDER_WWW
folder_npm     (env)  : FOLDER_NPM

// A custom Total.js Code editor instance identifier
uid            (env)  : UID

// Example 1 with the specific hostname: https://{0}.eu.totaljs.com
// Example 2 with the current hostname: {0}
origin         (env)  : ORIGIN

// Disables editing URL address: "true" or "false"
cloud          (env)  : CLOUD

// Limits max. count of projects
limit          (env)  : LIMIT

default_uid           : f